% ROI analysis


  G1 = ROIhits;
% sort cells
c = squeeze(mean(G1(:,150:250,:),1));
c2 = mean(c)
[m,ind] = max(c2);
[m2, ind2] = sort(ind,'ascend'); 
B1 = ind2(1:300);


figure(); 
for i = 1:100
 plot(G1(1:size(G1,1),:,B1(i))');
 pause(0.3)
end

