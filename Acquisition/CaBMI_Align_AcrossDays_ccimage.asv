function [score] = CaBMI_Align_AcrossDays_ccimage(I, pl)


    

[ccimage_01]=CrossCorrImage(I(:,:,10:end));

% gram frame
X = pl.PixelsPerLine(); % get x dim of the image
Y = pl.LinesPerFrame(); % get y dim of the image


for i = 1:200
current(:,:,i) = double( pl.GetImage_2(2,X,Y)); % Build the image
pause(0.01);
end


[ccimage_02]=CrossCorrImage(current(:,:,10:end));

figure(1);
RGB1 = CaBMI_XMASS(ccimage_02,ccimage_01,ccimage_02);
image(squeeze(RGB1(:,:,1,:)));
